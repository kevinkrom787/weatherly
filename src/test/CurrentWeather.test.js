import React from 'react';
import CurrentWeatherTest from '../lib/CurrentWeather.js';

describe('CurrentWeather Test', () => {

  it('should instanticate an new CurrentWeather', () => {
    expect(null).toEqual(null);
  });

});