import React from 'react';
import { shallow, mount, render } from 'enzyme';
global.localStorage = {}

import sevenHourForecast from '../lib/Seven-Hour-Forecast';
import CurrentWeather from '../lib/Current-Weather';
import tenDayForecast from '../lib/Ten-Day-Forecast';

describe('sevenHourForecast Test', () => {

  let renderedSevenHourForecast;
  beforeEach(() => renderedApp = shallow(<sevenHourForecast />));

  describe('sevenHourForecast should return an hour prop', () => {


  })
})
