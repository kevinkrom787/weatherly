import React from 'react';
import Welcome from '../lib/Welcome.js';

describe('Welcome Test', () => {

  it('should instanticate an new Welcome component', () => {
    expect(null).toEqual(null);
  });

});